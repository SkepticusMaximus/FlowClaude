[build]
builder = "nixpacks"
buildCommand = "cd mcp-server && npm install"

[deploy]
startCommand = "cd mcp-server && node http-server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
